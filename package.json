{
  "name": "naloga",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "mocha --no-timeouts --parallel --reporter mochawesome --require mochawesome/register --reporter-options reportDir=testReports,reportFilename=testResults"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mocha": "^9.1.3",
    "mochawesome-report-generator": "^6.0.1",
    "selenium-webdriver": "^4.0.0"
  },
  "devDependencies": {
    "mochawesome": "^7.0.1"
  }
}
